groups:
  - name: master
    jobs:
      - aws-uc-feature-development
      - aws-uc-feature-qa
      - aws-uc-feature-int
      - aws-uc-feature-preprod
      - aws-uc-feature-prod

  - name: pull-request
    jobs:
      - aws-uc-feature-pr

  - name: update-pipeline
    jobs:
      - update-pipeline

